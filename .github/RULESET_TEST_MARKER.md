# Ruleset verification marker

This file exists only to verify whether a PR merge and cocogitto's direct push
to `main` succeed under branch protection rulesets matching production.
Safe to delete once the verification is complete.
